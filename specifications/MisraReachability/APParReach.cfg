\* Use INIT/NEXT directly because the original Spec adds per-process
\* WF fairness which Apalache does not handle.

CONSTANTS
  Nodes <- NodesVal
  Succ <- SuccVal
  Root <- RootVal
  Procs <- ProcsVal

INIT Init
NEXT Next

INVARIANT
  Inv
