\* Use INIT/NEXT directly because the original Spec adds WF fairness
\* which Apalache does not handle.
\* `TypeOK` references an unbounded `Seq(...)`; we only check `Inv`.

CONSTANT Peers <- PeersVal

INIT Init
NEXT Next

INVARIANT Inv
