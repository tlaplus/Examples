\* The original LSpec is `HC /\ WF_hr(HCnxt)` where `HC` is itself
\* `HCini /\ [][HCnxt]_hr`. Apalache wants the canonical form
\* `Init /\ [][Next]_vars`, so we set Init/Next directly.

INIT HCini

NEXT HCnxt

INVARIANT HCini
