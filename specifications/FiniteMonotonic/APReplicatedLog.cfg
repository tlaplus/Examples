\* `TypeOK` references `Seq(Transaction)` (an unbounded set) which
\* Apalache cannot enumerate; we only check `Safety` here.

CONSTANTS
  Node <- NodeVal
  Transaction <- TransactionVal

INVARIANT Safety

SPECIFICATION Spec
