CONSTANTS
  CalculateHash <- CalculateHashImpl
  Hash = {h1, h2, h3, h4}
  NoHashVal = NoHashVal
  PrivateKey = {prv1, prv2}
  PublicKey = {pub1, pub2}
  Node = {n1, n2}
  GenesisBalance = 3
  NoBlockVal = NoBlockVal

CONSTANTS
  NoHash = [Nano]NoHashVal
  NoBlock = [Nano]NoBlockVal

VIEW
  View

SPECIFICATION
  Spec

INVARIANTS
  TypeInvariant
  SafetyInvariant

