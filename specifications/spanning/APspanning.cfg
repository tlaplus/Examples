\* Use INIT/NEXT directly because the original Spec adds WF fairness
\* which Apalache does not handle.

CONSTANTS
  Proc <- ProcVal
  NoPrnt <- NoPrntVal
  root <- rootVal
  nbrs <- nbrsVal

INIT Init
NEXT Next

INVARIANT TypeOK
