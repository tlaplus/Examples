\* The original Spec includes per-process WF fairness (`\A self \in 1..NP :
\* WF_vars(Philosopher(self))`); Apalache rejects that form, so we drive
\* the safety check from `Init`/`Next` directly.

CONSTANT NP = 5

INIT Init
NEXT Next

INVARIANT
  TypeOK
  ExclusiveAccess
